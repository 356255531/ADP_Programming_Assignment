def plot_policy(policy_sets, labels):
    pass


if __name__ == "__main__":
    import matplotlib.pyplot as plt
    import numpy as np

    # Simple data to display in various forms
    x = np.linspace(0, 2 * np.pi, 400)
    y = np.sin(x ** 2)
    plt.close('all')
    plt.show()
